{% extends 'base.html.twig' %}

{% block title %}New Project{% endblock %}

{% block body %}
    <h1>Create new Project</h1>

    {{ include('project/_form.html.twig') }}
    <div class="text-right">
        <a href="{{ path('project_index') }}" class="btn btn-sm mt-5">back to list</a>
    </div>
{% endblock %}
